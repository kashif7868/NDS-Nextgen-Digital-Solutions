import HeroSection from "./HeroSection";

const Home = () => {
  return (
    <main>
      <HeroSection />
      {/* Future sections: Services, About, Portfolio, Contact CTA */}
    </main>
  );
};

export default Home;